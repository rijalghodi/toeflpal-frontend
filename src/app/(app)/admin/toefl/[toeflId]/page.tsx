'use client';

import { Stack } from '@mantine/core';
import React, { Suspense } from 'react';

export default function ToeflDetailPage() {
  return (
    <Stack>
      <Suspense>TOEFL Detail</Suspense>
    </Stack>
  );
}
